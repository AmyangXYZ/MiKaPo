function Animation({ style }: { style: React.CSSProperties }): JSX.Element {
  return (
    <div className="animation" style={style}>
      Animation
    </div>
  )
}

export default Animation
